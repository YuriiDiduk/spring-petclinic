pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  
 stage('DockerLint') {
      steps {
        
        sh 'docker run --rm -i hadolint/hadolint < Dockerfile'
       
      }
    }
 
  }
